# 🏗️ OliveGuard System Architecture

## 📊 System Diagrams

This document provides visual representations of the OliveGuard system architecture, data flow, and component interactions.

---

## 1. 🎯 High-Level System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        OliveGuard System                        │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│                  │         │                  │         │                  │
│   Mobile App     │◄───────►│   Backend API    │◄───────►│   ML Model       │
│   (React Native) │  HTTP   │   (FastAPI)      │  Load   │   (TensorFlow)   │
│                  │  REST   │                  │         │                  │
└──────────────────┘         └──────────────────┘         └──────────────────┘
         │                            │                            │
         │                            │                            │
         ▼                            ▼                            ▼
┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│  AsyncStorage    │         │  SQLite DB       │         │  Model Files     │
│  (Local Data)    │         │  (Users)         │         │  (.tflite, .pth) │
└──────────────────┘         └──────────────────┘         └──────────────────┘
```

### Components:

1. **Mobile App (Frontend)**
   - React Native with Expo
   - TypeScript
   - AsyncStorage for offline data

2. **Backend API (Server)**
   - FastAPI framework
   - JWT authentication
   - Image processing

3. **ML Model (AI Engine)**
   - TensorFlow Lite inference
   - Disease classification
   - Confidence scoring

4. **Databases**
   - SQLite: User accounts
   - AsyncStorage: App data (trees, scans, alerts)

---

## 2. 📱 Frontend Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                         React Native App                           │
└────────────────────────────────────────────────────────────────────┘

                              App.tsx (Root)
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
              ┌─────▼─────┐                  ┌─────▼─────┐
              │  Auth     │                  │   Main    │
              │  Stack    │                  │   Tabs    │
              └───────────┘                  └───────────┘
                    │                               │
        ┌───────────┴───────────┐       ┌──────────┼──────────┬──────────┐
        │                       │       │          │          │          │
   ┌────▼────┐           ┌─────▼────┐  │          │          │          │
   │ Login   │           │  Signup  │  │          │          │          │
   └─────────┘           └──────────┘  │          │          │          │
                                        │          │          │          │
                            ┌───────────▼──┐  ┌───▼────┐  ┌──▼──────┐  │
                            │  Dashboard   │  │  Scan  │  │ Orchard │  │
                            │   Stack      │  │ Stack  │  │  Stack  │  │
                            └──────────────┘  └────────┘  └─────────┘  │
                                                                        │
                                              ┌─────────────┬───────────▼───┐
                                              │  Resources  │   Profile     │
                                              │    Stack    │    Stack      │
                                              └─────────────┴───────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                          Shared Contexts                                │
├─────────────────────────────────────────────────────────────────────────┤
│  AuthContext  │  ThemeContext  │  (Global State Management)            │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                          Utility Services                               │
├─────────────────────────────────────────────────────────────────────────┤
│  authService.ts  │  aiService.ts  │  storage.ts  │  (API & Storage)    │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                          Reusable Components                            │
├─────────────────────────────────────────────────────────────────────────┤
│  Button  │  Card  │  ThemedText  │  ThemedView  │  (UI Components)     │
└─────────────────────────────────────────────────────────────────────────┘
```

### Screen Flow:

**Unauthenticated:**
```
Login Screen ←→ Signup Screen
```

**Authenticated:**
```
Dashboard → Scan → Result → Orchard → Resources → Profile
    ↓         ↓       ↓         ↓          ↓         ↓
  Alerts   Camera  Assign    Tree      Article   Settings
           Gallery  to Tree  Details    Detail    Edit Profile
```

---

## 3. 🔐 Authentication Flow

```
┌──────────┐                                           ┌──────────┐
│  Mobile  │                                           │ Backend  │
│   App    │                                           │   API    │
└────┬─────┘                                           └────┬─────┘
     │                                                      │
     │  1. POST /auth/register                             │
     │     { email, password, name }                       │
     ├────────────────────────────────────────────────────►│
     │                                                      │
     │                                  2. Hash password    │
     │                                  3. Create user      │
     │                                  4. Generate JWT     │
     │                                                      │
     │  5. { access_token, user }                          │
     │◄────────────────────────────────────────────────────┤
     │                                                      │
     │  6. Store token in AsyncStorage                     │
     │     Store user data                                 │
     │                                                      │
     │  7. Navigate to Main App                            │
     │                                                      │
     │                                                      │
     │  8. POST /auth/login                                │
     │     { email, password }                             │
     ├────────────────────────────────────────────────────►│
     │                                                      │
     │                                  9. Verify password  │
     │                                 10. Generate JWT     │
     │                                                      │
     │  11. { access_token, user }                         │
     │◄────────────────────────────────────────────────────┤
     │                                                      │
     │                                                      │
     │  12. GET /auth/me                                   │
     │      Authorization: Bearer <token>                  │
     ├────────────────────────────────────────────────────►│
     │                                                      │
     │                                 13. Verify JWT       │
     │                                 14. Get user data    │
     │                                                      │
     │  15. { user data }                                  │
     │◄────────────────────────────────────────────────────┤
     │                                                      │
     │                                                      │
     │  16. POST /auth/logout                              │
     │      Authorization: Bearer <token>                  │
     ├────────────────────────────────────────────────────►│
     │                                                      │
     │                                 17. Invalidate token │
     │                                                      │
     │  18. Success                                        │
     │◄────────────────────────────────────────────────────┤
     │                                                      │
     │  19. Clear AsyncStorage                             │
     │  20. Navigate to Login                              │
     │                                                      │
```

---

## 4. 🔬 Disease Detection Flow

```
┌────────────────────────────────────────────────────────────────────────┐
│                    Disease Detection Pipeline                          │
└────────────────────────────────────────────────────────────────────────┘

   User                Mobile App              Backend API          ML Model
    │                      │                        │                  │
    │  1. Take Photo      │                        │                  │
    │─────────────────────►│                        │                  │
    │                      │                        │                  │
    │                      │  2. Compress Image     │                  │
    │                      │     (JPEG, < 5MB)      │                  │
    │                      │                        │                  │
    │                      │  3. POST /analyze      │                  │
    │                      │     + image file       │                  │
    │                      │     + JWT token        │                  │
    │                      ├───────────────────────►│                  │
    │                      │                        │                  │
    │                      │                        │  4. Validate JWT │
    │                      │                        │     token        │
    │                      │                        │                  │
    │                      │                        │  5. Load image   │
    │                      │                        │     (PIL)        │
    │                      │                        │                  │
    │                      │                        │  6. Preprocess:  │
    │                      │                        │     - Resize     │
    │                      │                        │     - Normalize  │
    │                      │                        │     - Padding    │
    │                      │                        │                  │
    │                      │                        │  7. Run inference│
    │                      │                        ├─────────────────►│
    │                      │                        │                  │
    │                      │                        │     TensorFlow   │
    │                      │                        │     Lite model   │
    │                      │                        │     processing   │
    │                      │                        │                  │
    │                      │                        │  8. Predictions  │
    │                      │                        │◄─────────────────┤
    │                      │                        │   [0.02, 0.03,   │
    │                      │                        │    0.95]         │
    │                      │                        │                  │
    │                      │                        │  9. Map to class │
    │                      │                        │     - healthy    │
    │                      │                        │     - aculus     │
    │                      │                        │     - peacock    │
    │                      │                        │                  │
    │                      │                        │ 10. Get max      │
    │                      │                        │     confidence   │
    │                      │                        │                  │
    │                      │ 11. Response:          │                  │
    │                      │ { diagnosis,           │                  │
    │                      │   confidence,          │                  │
    │                      │   disease_type,        │                  │
    │                      │   recommendations }    │                  │
    │                      │◄───────────────────────┤                  │
    │                      │                        │                  │
    │                      │ 12. Display result     │                  │
    │                      │     + confidence       │                  │
    │                      │     + treatment        │                  │
    │                      │                        │                  │
    │  13. View Result    │                        │                  │
    │◄─────────────────────┤                        │                  │
    │                      │                        │                  │
    │                      │ 14. Save to           │                  │
    │                      │     AsyncStorage       │                  │
    │                      │     (scan history)     │                  │
    │                      │                        │                  │
```

### Image Preprocessing:

```
Original Image (Any size)
         │
         ▼
┌─────────────────┐
│  Resize         │  → 224x224 pixels
└────────┬────────┘
         ▼
┌─────────────────┐
│  RGB to Array   │  → [224, 224, 3]
└────────┬────────┘
         ▼
┌─────────────────┐
│  Normalize      │  → [0-1] range
└────────┬────────┘
         ▼
┌─────────────────┐
│  Batch          │  → [1, 224, 224, 3]
└────────┬────────┘
         ▼
   Model Input
```

---

## 5. 💾 Data Storage Architecture

```
┌────────────────────────────────────────────────────────────────────────┐
│                         Data Storage Layers                            │
└────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐                 ┌─────────────────────┐
│   Mobile App        │                 │   Backend Server    │
├─────────────────────┤                 ├─────────────────────┤
│                     │                 │                     │
│  AsyncStorage       │                 │   SQLite Database   │
│  ┌───────────────┐  │                 │  ┌───────────────┐  │
│  │ @olive_       │  │                 │  │ users table   │  │
│  │ auth_token    │  │                 │  ├───────────────┤  │
│  ├───────────────┤  │                 │  │ id            │  │
│  │ @olive_user   │  │                 │  │ email         │  │
│  ├───────────────┤  │                 │  │ hashed_       │  │
│  │ @oliveguard_  │  │                 │  │ password      │  │
│  │ trees         │  │                 │  │ name          │  │
│  ├───────────────┤  │                 │  │ created_at    │  │
│  │ @oliveguard_  │  │                 │  └───────────────┘  │
│  │ scans         │  │                 │                     │
│  ├───────────────┤  │                 │  olive_app.db       │
│  │ @oliveguard_  │  │                 └─────────────────────┘
│  │ alerts        │  │
│  ├───────────────┤  │
│  │ @oliveguard_  │  │
│  │ user          │  │
│  └───────────────┘  │
└─────────────────────┘

Storage Type: Key-Value Pairs       Storage Type: Relational
Persistence: Local Device           Persistence: Server
Size Limit: ~6MB (iOS), ~10MB (Android)  Size Limit: Unlimited
```

### Data Models:

**Frontend (AsyncStorage):**
```typescript
Tree {
  id: string
  name: string
  location: string
  plantedDate: string
  healthStatus: 'healthy' | 'at-risk' | 'diseased'
  lastScannedAt?: string
}

Scan {
  id: string
  treeId?: string
  treeName?: string
  imageUri: string
  heatmapUri?: string
  diagnosis: string
  diseaseType: 'healthy' | 'pest' | 'fungal'
  confidenceScore: number
  recommendations: string[]
  scannedAt: string
  isAnomaly: boolean
}

Alert {
  id: string
  treeId: string
  treeName: string
  scanId: string
  diagnosis: string
  severity: 'low' | 'medium' | 'high'
  timestamp: string
  isRead: boolean
  thumbnailUri?: string
}

UserProfile {
  id: string
  name: string
  email: string
  orchardName?: string
  createdAt: string
}
```

**Backend (SQLite):**
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT UNIQUE NOT NULL,
    hashed_password TEXT NOT NULL,
    name TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 6. 🔄 API Endpoints Map

```
┌────────────────────────────────────────────────────────────────────────┐
│                        FastAPI Backend Server                          │
│                        http://localhost:8000                           │
└────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  Authentication Endpoints                                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  POST   /auth/register    Create new user account                  │
│         Body: { email, password, name }                            │
│         Returns: { access_token, token_type, user }                │
│                                                                     │
│  POST   /auth/login       Authenticate user                        │
│         Body: { email, password }                                  │
│         Returns: { access_token, token_type, user }                │
│                                                                     │
│  GET    /auth/me          Get current user info                    │
│         Headers: Authorization: Bearer <token>                     │
│         Returns: { id, email, name }                               │
│                                                                     │
│  POST   /auth/logout      Invalidate token                         │
│         Headers: Authorization: Bearer <token>                     │
│         Returns: { message: "Logged out" }                         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  Analysis Endpoints                                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  POST   /analyze          Analyze leaf image                       │
│         Headers: Authorization: Bearer <token>                     │
│         Body: multipart/form-data { file: <image> }                │
│         Returns: {                                                 │
│           diagnosis: string                                        │
│           confidence: float (0-1)                                  │
│           disease_type: string                                     │
│           recommendations: string[]                                │
│         }                                                          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  System Endpoints                                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  GET    /health           Check server health                      │
│         Returns: {                                                 │
│           status: "ok"                                             │
│           model_loaded: boolean                                    │
│           model: "efficientnet_lite0"                              │
│         }                                                          │
│                                                                     │
│  GET    /info             Get model info                           │
│         Returns: {                                                 │
│           model_name: string                                       │
│           version: string                                          │
│           classes: string[]                                        │
│         }                                                          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 7. 🎨 Component Hierarchy

```
App
└── ErrorBoundary
    └── SafeAreaProvider
        └── GestureHandlerRootView
            └── KeyboardProvider
                └── AuthProvider
                    └── NavigationContainer
                        └── AppNavigator
                            ├── AuthStackNavigator (if not authenticated)
                            │   ├── LoginScreen
                            │   └── SignupScreen
                            │
                            └── MainTabNavigator (if authenticated)
                                ├── DashboardStackNavigator (Tab 1)
                                │   ├── DashboardScreen
                                │   ├── AlertsScreen
                                │   └── ScanResultScreen
                                │
                                ├── ScanStackNavigator (Tab 2)
                                │   ├── ScanScreen
                                │   └── ScanResultScreen
                                │
                                ├── OrchardStackNavigator (Tab 3)
                                │   ├── OrchardScreen
                                │   ├── AddTreeScreen
                                │   ├── TreeDetailScreen
                                │   └── TreeEditScreen
                                │
                                ├── ResourcesStackNavigator (Tab 4)
                                │   ├── ResourcesScreen
                                │   └── ResourceDetailScreen
                                │
                                └── ProfileStackNavigator (Tab 5)
                                    ├── ProfileScreen
                                    ├── EditProfileScreen
                                    └── SettingsScreen
```

---

## 8. 🌐 Network Communication

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Network Layer Architecture                      │
└─────────────────────────────────────────────────────────────────────┘

Mobile App                            Backend Server
    │                                      │
    │  HTTP Request                        │
    │  ─────────────────────────────────► │
    │  Method: POST                        │
    │  URL: /analyze                       │
    │  Headers:                            │
    │    Authorization: Bearer <JWT>       │
    │    Content-Type: multipart/form-data │
    │  Body:                               │
    │    file: <image_binary>              │
    │                                      │
    │                        ┌─────────────┴─────────────┐
    │                        │  Request Processing       │
    │                        ├───────────────────────────┤
    │                        │  1. Verify JWT            │
    │                        │  2. Validate image format │
    │                        │  3. Process image         │
    │                        │  4. Run ML inference      │
    │                        │  5. Format response       │
    │                        └─────────────┬─────────────┘
    │                                      │
    │  HTTP Response                       │
    │  ◄────────────────────────────────── │
    │  Status: 200 OK                      │
    │  Content-Type: application/json      │
    │  Body:                               │
    │  {                                   │
    │    "diagnosis": "Aculus Olearius",   │
    │    "confidence": 0.95,               │
    │    "disease_type": "pest",           │
    │    "recommendations": [...]          │
    │  }                                   │
    │                                      │
    ▼                                      ▼
```

### Error Handling:

```
┌──────────────────────────┐
│  Network Error Handling  │
├──────────────────────────┤
│                          │
│  401 Unauthorized        │ → Redirect to Login
│  403 Forbidden           │ → Show error message
│  404 Not Found           │ → Show "Resource not found"
│  422 Validation Error    │ → Show validation errors
│  500 Server Error        │ → Show "Try again later"
│  Network Timeout         │ → Retry with exponential backoff
│  No Internet Connection  │ → Use offline mode/cache
│                          │
└──────────────────────────┘
```

---

## 9. 🔒 Security Architecture

```
┌────────────────────────────────────────────────────────────────────────┐
│                        Security Layers                                 │
└────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│  Transport Layer    │  → HTTPS (TLS/SSL encryption)
├─────────────────────┤
│  Authentication     │  → JWT tokens (RS256/HS256)
├─────────────────────┤
│  Password Security  │  → bcrypt hashing (cost factor 12)
├─────────────────────┤
│  Token Storage      │  → AsyncStorage (encrypted on device)
├─────────────────────┤
│  API Authorization  │  → Bearer token in Authorization header
├─────────────────────┤
│  Input Validation   │  → Server-side validation
├─────────────────────┤
│  File Upload        │  → Size limit, type checking
├─────────────────────┤
│  Database           │  → SQL injection protection
└─────────────────────┘

JWT Token Structure:
┌──────────────────────────────────────┐
│ Header                               │
│ { "alg": "HS256", "typ": "JWT" }     │
├──────────────────────────────────────┤
│ Payload                              │
│ {                                    │
│   "sub": "user@email.com",           │
│   "exp": 1234567890,                 │
│   "iat": 1234567890                  │
│ }                                    │
├──────────────────────────────────────┤
│ Signature                            │
│ HMACSHA256(                          │
│   base64UrlEncode(header) + "." +    │
│   base64UrlEncode(payload),          │
│   secret                             │
│ )                                    │
└──────────────────────────────────────┘
```

---

## 10. 📈 Scalability Considerations

```
Current Architecture:
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Mobile App │────►│   Backend   │────►│   SQLite    │
└─────────────┘     └─────────────┘     └─────────────┘

Future Scalable Architecture:
┌─────────────┐     ┌──────────────┐     ┌──────────────┐     ┌─────────────┐
│  Mobile App │────►│ Load Balancer│────►│   Backend    │────►│  PostgreSQL │
└─────────────┘     └──────────────┘     │   Cluster    │     └─────────────┘
                                         └──────┬───────┘
                                                │
                                         ┌──────▼───────┐
                                         │  Redis Cache │
                                         └──────────────┘
                                                │
                                         ┌──────▼───────┐
                                         │  S3 Storage  │
                                         └──────────────┘
```

---

## 📚 Legend

```
Symbols Used:
  ────►   Data flow / HTTP request
  ◄────   Response / Return value
  │       Vertical connection
  ├──     Branch / Split
  └──     End of branch
  ┌──┐    Box / Container
  ▼       Downward flow
  ►       Rightward flow
```

---

## 📝 Notes

- All diagrams represent the current state of the system
- Authentication uses JWT tokens with HS256 algorithm
- Images are processed with TensorFlow Lite for mobile optimization
- AsyncStorage is used for offline-first architecture
- Backend is stateless and horizontally scalable

---

Created: November 28, 2025
Version: 1.0
Author: JedidiAymen
